# 复用指南

这套东西**可以复用**，而且是按「能复用」来设计的。分三个层次，从最省事到最折腾：

| 场景 | 要改什么 | 难度 |
|---|---|---|
| A. Antigravity 更新后主题没了 | 什么都不用改，重跑一次装 | 30 秒 |
| B. 想换个配色 / 换个字体 | 只改 `pixel.css` | 10 分钟 |
| C. 拿去美化别的 Electron 应用 | 改 `install.ps1` 的三个锚点 | 半小时到几小时 |

---

## A. Antigravity 更新后重新装回来

Antigravity 自动更新会把 `app.asar` 换成原版，主题就没了。这是**唯一**需要定期做的事：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1 -KillRunning
```

脚本设计成可以无限次重复运行：
- 检测到已经打过补丁 → 只刷新主题资源，不重复打补丁
- 检测到是原版 → 完整走一遍打补丁流程

**如果更新后脚本报 `Patch anchor not found`**，说明新版 Antigravity 改了 `dist/utils.js` 的结构。
脚本这时候**什么都不会改**直接退出（不会留下半坏状态）。修法见下面 C 节的「锚点怎么找」。

---

## B. 换配色 / 换字体

### 换配色

主题装在这里，是纯文本 CSS：

```
%LOCALAPPDATA%\Programs\Antigravity\resources\pixel-theme\pixel.css
```

改完保存，回 Antigravity 按 **Ctrl+R** 刷新即可，不用重装不用重启。

**只需要改文件开头 `:root` 里那组 `--px-*` 变量**，全局自动跟着变：

```css
:root {
  --px-navy:    #1a1c2c;   /* 主背景（暗色） */
  --px-white:   #f4f4f4;   /* 主文字（暗色） */
  --px-cyan:    #73eff7;   /* 强调色：焦点框、链接、主按钮 */
  --px-gray:    #566c86;   /* 边框 */
  --px-paper:   #eef0f5;   /* 主背景（亮色） */
  ...
}
```

想整套换风格，推荐几组现成的复古调色板（搜 Lospec 能找到色值）：

- **Game Boy 绿**：`#0f380f` / `#306230` / `#8bac0f` / `#9bbc0f`
- **CGA 洋红**：`#000000` / `#55ffff` / `#ff55ff` / `#ffffff`
- **琥珀单色终端**：`#1a1200` / `#ffb000` / `#ffcc55`
- **Nord**（冷淡风）：`#2e3440` / `#88c0d0` / `#eceff4`

> 改完记得把 `pixel.css` 拷回项目里的 `pixel-theme\pixel.css` 存一份，
> 否则下次重装会被覆盖掉。

### 换字体

1. 找一个 `.woff2` 格式的像素字体，丢进 `pixel-theme\fonts\`
2. 打开 `patch\pixelTheme.js`，改最上面的 `FONTS` 表：

```js
const FONTS = {
    ui:   { file: "你的字体.woff2", family: "字体的 PostScript 名" },
    mono: { file: "你的等宽字体.woff2", family: "..." },
};
```

3. 再改 `pixel.css` 里 `--px-font-ui` / `--px-font-mono` 的字体名
4. 重跑 `install.ps1`

**选字体的坑**：中文点阵字体不多，且必须是**点阵/位图轮廓**的（比如缝合怪、Zpix、
未来熙墨），普通字体缩小了不叫像素风。另外字体的 `family` 名字必须写对，
可以用 `fc-query` 或在线工具查，写错了就静默 fallback 到系统字体。

---

## C. 拿去美化别的 Electron 应用

这才是这套脚本真正能复用的地方。**核心思路是通用的**，只有三个锚点是 Antigravity 专属的。

### 先判断目标应用能不能改

按顺序检查，任何一条不满足就放弃：

**1. 是不是 Electron 应用？**
安装目录里有 `resources\app.asar` 和一堆 `.dll`（`libGLESv2.dll`、`ffmpeg.dll`）就是。

**2. asar 完整性校验关了吗？**（关键！）
```powershell
node -e "const fs=require('fs');const b=fs.readFileSync('目标\\App.exe');const i=b.indexOf('dL7pKGdnNz796PbbjQWNKmHXBZaB9tsX');const names=['RunAsNode','EnableCookieEncryption','EnableNodeOptionsEnvironmentVariable','EnableNodeCliInspectArguments','EnableEmbeddedAsarIntegrityValidation','OnlyLoadAppFromAsar','LoadBrowserProcessSpecificV8Snapshot','GrantFileProtocolExtraPrivileges'];const c=b[i+33];for(let k=0;k<c;k++)console.log(names[k]||k,'=',String.fromCharCode(b[i+34+k]))"
```
看 `EnableEmbeddedAsarIntegrityValidation`：
- `= 0` → 可以改（Antigravity 就是 0）
- `= 1` → **改了 asar 应用会拒绝启动**，这条路走不通

**3. 界面是不是网页？**
是就能用 CSS 改。Electron 应用基本都是。

### 然后：找注入锚点

打开 `install.ps1`，里面有三个 `$A1/$A2/$A3` 锚点，是从 Antigravity 的
`dist/utils.js` 里扒出来的。换个应用就要重找。

先把目标的 asar 拆开看：
```powershell
npx @electron/asar extract "目标\resources\app.asar" .\拆包
```

然后在拆出来的代码里搜 `new BrowserWindow`，找到创建窗口的那个函数。你需要三个位置：

| 锚点 | 找什么 | 干什么用 |
|---|---|---|
| `$A1` | 文件顶部任意一行 `require(...)` | 在它后面插入 `require("./pixelTheme")` |
| `$A3` | `win.loadURL(...)` 那一行 | 在它**前面**插一句 `attachPixelTheme(win)` |
| `$A2` | （可选）原本的加载动画调用 | 换成像素版加载动画 |

只要 `$A1` 和 `$A3` 就能工作，`$A2` 是锦上添花。**锚点必须在文件里唯一**，
否则 `.Replace()` 会把所有匹配的地方都换掉。

改完 `install.ps1` 里这几行：

```powershell
$A1 = '这里填你找到的 require 行'
$A2 = '这里填加载动画调用'
$A3 = '这里填 win.loadURL 那行'
```

以及顶部的路径：

```powershell
$AppDir = Join-Path $env:LOCALAPPDATA 'Programs\你的应用名'
```

### 最后：重写 CSS

**别逐个组件去覆盖样式**——那样又累又脆，应用一升级就全崩。
正确做法是先找到目标应用的**主题变量**，直接改变量的值。

用 Chrome DevTools 协议连上去看（应用得带 `--remote-debugging-port=9222` 启动，
或者去 `%APPDATA%\应用名\DevToolsActivePort` 读它自动分配的端口）：

```js
// 在 DevTools Console 里跑，看看有哪些主题变量
const st = document.documentElement.style;
const out = {};
for (let i = 0; i < st.length; i++) {
  const n = st[i];
  if (n.startsWith('--')) out[n] = st.getPropertyValue(n);
}
console.log(out);

// 再看看 body 上继承下来的
console.log(getComputedStyle(document.body).getPropertyValue('--background'));
```

Antigravity 的情况是：Tailwind v4 + 16 个语义变量（`--background`、`--foreground`、
`--primary`……）+ 445 个 `--vscode-*` 变量，全部内联在 `<html>` 上。
所以 `pixel.css` 干的事就是把这些变量重新赋值。

**注意**：变量是内联在 `<html>` 上的，内联样式优先级高于样式表，
所以覆盖时**必须加 `!important`**。

VS Code / Cursor / Windsurf 系的应用用的是 `--vscode-*` 那一套，
`pixel.css` 里第 1、2 节可以直接拿去用。

---

## 踩过的坑（照抄能省时间）

这几个都是实际做的时候踩到的，代码里也都写了注释：

**1. 别用 `[class*="border"]` 加粗边框**

Tailwind 里 `border-l` 只有左边框。用 `[class*="border"]` 匹配会给它凭空补出四条边。
要用 `[class~="border"]`（精确匹配完整类名），单边的分开写：

```css
[class~="border"]   { border-width: 2px !important; }
[class~="border-t"] { border-top-width: 2px !important; }
```

**2. 别给空浮层加边框，会把整个页面撑出滚动条**

应用里常有 0×0 的常驻空占位浮层（比如输入框的联想菜单占位）。给它加 2px 边框
就变成 4×4 顶在文档底部 → 文档溢出 → 出现滚动条 → 可视区变窄 → 但根容器是
`100vw/100vh`（含滚动条宽度）→ 永久溢出，整个界面能拖动，而系统窗口按钮
不跟随页面滚动，右上角就错位重叠了。

两道保险一起上：

```css
/* 保险 1：只给有内容的浮层加边框 */
[role="listbox"]:not(:empty) { border: 2px solid ...; }

/* 保险 2：桌面应用的文档层本来就不该滚动，直接焊死 */
html, body { overflow: hidden !important; width: 100% !important; height: 100% !important; }
```

**3. `scrollbar-color` 会让 `::-webkit-scrollbar` 整个失效**

Chromium 里只要元素设了 `scrollbar-color` 或 `scrollbar-width`，webkit 那套
滚动条伪元素就不生效了。而且 `scrollbar-color` 会被子元素继承——应用在 `<body>`
上内联一个，等于全局废掉。必须先撤回 `auto`：

```css
html, body, body * { scrollbar-color: auto !important; scrollbar-width: auto !important; }
```

**4. 像素字体必须关抗锯齿**

不然边缘发糊，白瞎了点阵字体：

```css
html, body, body * { -webkit-font-smoothing: none !important; }
```

**5. 像素字体没有真粗体**

用 `font-weight: bold` 会触发合成加粗，糊成一团。统一压到 400，靠颜色分层级：

```css
b, strong, h1, h2, h3, [class*="font-bold"] { font-weight: 400 !important; }
```

**6. 系统窗口按钮的底色，在 Windows 11 上根本改不动 —— 别浪费时间，自己画**

`titleBarStyle: 'hidden'` + `titleBarOverlay` 的应用，右上角最小化/关闭是**操作系统画的**，
CSS 管不着。文档说可以用 `titleBarOverlay.color` 指定颜色，**但实测无效**：

| 传入的颜色 | 实际渲染 |
|---|---|
| `#dde1ea`（想要的顶栏色） | `#EAEAEB` |
| `#1a1c2c`（深海军蓝） | `#EAEAEB` |
| `#00ff00`（亮绿，探针） | `#EAEAEB` |

构造函数里传、运行时调 `setTitleBarOverlay()` 都试过，Windows 一律画它自己的 `#EAEAEB`。
（测试环境：Windows 11 build 26200 / Electron 33+）

所以正确解法是**关掉系统按钮，在网页里自己画**，这样它天然就是顶栏的一部分：

```js
// 主进程：把 titleBarOverlay 关掉
// 原代码：  titleBarOverlay: isMacOS() ? false : { color, symbolColor, height }
// 改成：    titleBarOverlay: !wantsNativeCaption() || isMacOS() ? false : { ... }
```

```js
// 渲染进程：自己画三个按钮，驱动窗口
window.electronNative.minimize();
window.electronNative.maximize();   // 配合 isMaximized() / unmaximize() 做切换
window.electronNative.close();
```

**先去 `dist/preload.js` 里搜 `exposeInMainWorld`**——很多 Electron 应用（Antigravity 就是）
早就把这套窗口控制 API 暴露给渲染进程了，可以直接用，不用改 preload、不用加 IPC。
搜不到的话才需要自己加 `contextBridge` + `ipcMain.handle`。

图标别用字符（`─ □ ✕`），用 CSS 画方块和斜杠，任何缩放下都是硬边缘，才够像素。

**副作用**：关掉 overlay 后，应用自己若还在调 `window:set-title-bar-overlay`，
会抛 `Titlebar overlay is not enabled` 刷日志。在那个 IPC handler 的判断里加个标志位挡掉即可
（标志位直接挂在 `win` 对象上，这样 handler 所在的文件不用新增 import）。

**7. 正则扫 CSS 会匹配到你自己写的注释**

如果主进程要从 CSS 里读配置（比如读一个开关变量），记得**先剥掉注释再匹配**：

```js
const decls = css.replace(/\/\*[\s\S]*?\*\//g, '');
```

我就栽在这：CSS 里写的是 `--px-native-caption: off;`，但上面的注释里有一句
「改成 on 就换回系统按钮」，正则一扫就匹配到注释里那个 `on` 了，开关永远是开的，
排查了半天以为是 Electron 的问题。

**8. 别用 PowerShell 的 `Set-Content -Encoding utf8` 改 CSS**

Windows PowerShell 5.1 的 `Set-Content -Encoding utf8` 会**加 BOM，还会把 LF 转成 CRLF**。
文件平白胖一圈（我这 18988 → 19509 字节），而且 BOM 混在样式表中间会直接破坏 CSS 解析——
表现是整个主题静默失效，页面变成窗口底色。改文件用 `Copy-Item` 或明确指定无 BOM 编码。

**9. contextBridge 暴露的对象是冻结的**

想在渲染进程里把某个 API 方法替换成空函数？做不到：

```js
Object.isFrozen(window.electronNative)   // true
// 赋值静默失败，defineProperty 抛 "Cannot redefine property"
```

要改行为只能去主进程那一侧动手。

**10. 单实例锁**

调试时 `Start-Process 应用.exe --remote-debugging-port=9223` 可能没反应——
因为应用已经在跑，`requestSingleInstanceLock()` 把新实例挡掉了。
去 `%APPDATA%\应用名\DevToolsActivePort` 读第一行，那是它自己分配的端口，直接连。

**11. 做动效时：`scale()` 是点阵字体的天敌**

这条是加动效时最先撞上的墙。缩放会把点阵字重采样，动画中途先糊掉再变清楚，
非常难看——而现代 UI 库的开合动画默认就爱用 scale。Antigravity 的弹层原本是：

```css
/* 应用自带的：translateY 没问题，scaleY 会让弹层里的字糊一下 */
.animate-slideIn[data-starting-style] { opacity: 0; transform: translateY(-8px) scaleY(0.95); }
```

改法是整条换掉，**只留整数像素的位移**，位移量取跳帧数的整数倍
（比如 3 帧就用 6px，每帧刚好 2px，永远落在整数像素上）：

```css
.animate-slideIn[data-starting-style],
.animate-slideIn[data-ending-style] {
  opacity: 0 !important;
  transform: translateY(-6px) !important;   /* 不要 scale */
}
```

顺带一句：像素风的动效一律用 `steps()`，别用 `cubic-bezier`。
最省事的做法是一条全局规则接管所有过渡的插值函数，时长和属性都不用动：

```css
*, *::before, *::after { transition-timing-function: steps(4, end) !important; }
```

这一条就能把应用里所有现成的 `transition-colors` / `transition-opacity`
连带侧栏伸缩那种 0.5s 的平滑位移，一次性全变成跳格。

**12. 动画里改位移，用 `translate` 而不是 `transform`**

`transform` 是一整个属性，动画一接管就把元素原本的 `transform` 抹掉了。
最容易踩的是图标：展开箭头本来带着 `rotate(90deg)`，你给它加个悬停弹跳，
一悬停箭头就转回去了。

用独立的 `translate` 属性就没这问题，它和 `transform` 是可叠加的：

```css
@keyframes px-jiggle { 50% { translate: 0 -2px; } }   /* 不要写 transform: translateY(-2px) */
```

同理，覆盖应用自带的关键帧（同名后定义即覆盖）时也该用 `translate`——
你并不知道它被挂在什么元素上，那元素可能本来就有 `transform`。

**13. `!important` 的优先级高于 CSS 动画**

想给焦点框做个闪烁，改 `outline-color` 死活不动——因为焦点框那条规则是
`outline: 2px solid var(--primary) !important`，而 `!important` 声明**压得住动画**。

要么去掉那个 `!important`（但它通常正是用来盖应用样式的，去不掉），
要么换一个没被 `!important` 占用的属性。这里用的是 `filter`：

```css
@keyframes px-focus-flash { 50% { filter: var(--px-flash); } }
```

`--px-flash` 在亮暗两套配色里分别给 `brightness(0.88)` 和 `brightness(1.45)`——
亮色模式往亮里闪会直接洗成白块看不见，必须往暗里走。

**14. `insertCSS` 注入的样式表，在 `document.styleSheets` 里看不到**

调试时想确认「我的规则到底注进去了没有」，扫 `document.styleSheets` 是白扫的：
`webContents.insertCSS()` 走的是 WebFrame 的注入样式表，不进 `document.styleSheets`。
我照这个思路验了一轮，得出一堆「规则丢失」的假警报。

**唯一靠得住的验证方式是量计算值**（应用带 `--remote-debugging-port` 起，走 CDP）：

```js
getComputedStyle(document.body).transitionTimingFunction        // 'steps(4)' → 全局跳帧生效
getComputedStyle(document.documentElement, '::before').animationName  // 伪元素也能量
```

还有一个坑中坑：别用 `document.querySelector('选择器:hover')` 取「当前悬停的元素」——
它返回的是**文档顺序里第一个**匹配的，祖先链上任何一个匹配元素都算 `:hover`，
很容易量到隔壁去。要先把目标元素抓在手里，再 `el.matches(':hover')` 确认。

**15. `:has()` 反查祖先很贵，有等价的类名路径就别用**

给列表行加悬停效果时，行本身往往没有可辨识的类名。很自然会想到从子元素的
`data-testid` 反查：`[class*="cursor-pointer"]:has(> div > div > [data-testid^="convo-pill-"])`。
能用，而且看着比一串 Tailwind 类名可靠 —— 但代价很重：

| | 侧栏插一行+删一行 | 整文档样式重算 |
|---|---|---|
| 不加规则 | 1.52ms | 47ms |
| `:has()` 版 | **7.12ms** | 58ms |
| `.w-full.group > [class*="cursor-pointer"]` | 1.59ms | 47ms |

原因是 `:has()` 让**祖先随子树变动一起失效**，列表一重渲染就要重新求值。
流式输出时那一片是持续在动的，等于把这个代价乘在每一帧上。

先查一下有没有等价的祖先类名可用（这里行外面正好套着 `.w-full.group`），
两个选择器命中的是完全同一批元素 —— 写个断言确认再换：

```js
const a = [...document.querySelectorAll(贵的选择器)];
const b = [...document.querySelectorAll(便宜的选择器)];
a.length === b.length && a.every((x, i) => x === b[i])   // 必须 true 才能换
```

顺带一条同源的经验：`* { ... }` 也别乱写。把过渡的插值函数挂在 `*` 上比挂在
`[class*="transition"]` 上更贵（整文档重算 70ms vs 66ms，插入子树 3.3ms vs 1.8ms），
因为全应用只有 153/1384 个元素真的带过渡，剩下的都是白交钱。

**16. 测「空闲开销」千万别用 requestAnimationFrame 循环**

想量常驻动画在空闲时吃多少 CPU，第一反应是跑个 rAF 循环数帧顺便读指标。
**这个方法会直接造出假数字**：rAF 会强行把帧率顶满，逼着主线程每帧 tick 一次动画，

| 同一份 CSS，同一台机器 | style recalc | CPU |
|---|---|---|
| 用 rAF 循环测「空闲」 | 165 次/秒 | 430ms / 4 秒（≈ 单核 10%） |
| 用 `setTimeout` 静置测 | 8 次/秒 | 9ms / 2.5 秒（≈ 单核 0.4%） |

差 20 倍，而且是往「看起来很糟」的方向差。我照第一个数字断定动画有严重性能问题，
还据此改了代码，结果全是自己制造的。

正确做法是**什么都不做**，只用 `setTimeout` 等一段时间，前后读两次
CDP `Performance.getMetrics` 的 `RecalcStyleCount` / `RecalcStyleDuration` / `TaskDuration` 相减——
浏览器空闲时本来就只在需要时才出帧，这才是真实行为。

（也正因为这样，`steps()` 动画其实比平滑动画更省：`steps(28)` 意味着 7 秒里值只变
28 次，只有这 28 个时刻需要出帧。）

**17. 动 layout 的过渡，别做长动画 —— 加帧数也救不回来**

分清楚两类过渡，它们差一个数量级：

| | 动的属性 | 每帧的代价 |
|---|---|---|
| 便宜 | `transform` / `opacity` | 合成器，主线程几乎不参与 |
| 贵 | `width` / `height` / `grid-template-rows` / 各种 `--自定义宽度` | **每帧一次全应用重排** |

侧栏伸缩、面板折叠、手风琴展开全都属于第二类。实测 Antigravity 那个
500ms 的平滑侧栏动画（1.2 秒窗口）：

| | 超 33ms 的长帧 | 一次切换的 style recalc | 用户看到的格数 |
|---|---|---|---|
| 原版平滑 0.5s | 9 个 | 546ms | 8 格（还是忽大忽小的跳） |
| 改成跳帧 12 格 | 9 个 | 572ms | —— |
| **缩到 70ms / 2 帧** | **2 个** | **227ms** | 2 格 |

看第二行：**给它加帧数完全没用**，因为瓶颈是每帧重排，不是帧数不够。
而第一行说明这类动画天生就跑不满 —— 名义上 500ms 平滑，实际只画出 8 格。

结论（初版）：这类过渡要么保持原样别碰，要么**缩短到几十毫秒让它「啪」地到位**。
—— 这个结论后来被推翻了一半：还有第三条路，而且是最优解，见本条末尾的「后续」。

两个实现细节：

- **别用 `transition-duration: 0s`**。时长为 0 时 `transitionend` **不会触发**，
  如果应用在等这个事件来收尾状态，就会卡住。用 70ms 这种很短但非零的值。
- **面积决定帧数**。同样是 2 帧，2px 的位移是「咔哒」的手感，
  一块 256×293 的弹层分 2～3 帧淡入就是「在抖」。
  小控件放心跳帧，大面板要么给足帧数，要么干脆别做渐变直接弹出来。

怎么量「用户实际看到几格」：每帧记录被动画的那个值，数相邻不同值的个数。
这个数字比帧率有用得多 —— 帧率满 165fps 但只有 3 格，照样看着卡。

```js
// 在 rAF 里采样，动画结束后统计
const trace = [];
const tick = () => { trace.push(Math.round(el.getBoundingClientRect().x)); requestAnimationFrame(tick); };
// 相邻去重后的长度 = 视觉格数
```

**后续：真正的解法 —— 先归因每帧在算什么，再决定砍时长还是砍成本**

上面那「9 个长帧」后来查到了真正的元凶，而且不是 width 动画本身。
应用把过渡挂在**根节点内联样式的注册自定义属性**上：

```css
/* 根节点内联；两个变量都是 @property … inherits: true 注册的 */
transition: --aux-pane-width 500ms, --sidebar-width 500ms;
```

给根上的**继承型**自定义属性做动画 = 每帧全树样式失效（这里是 1384 个元素）。
而画面上真正的滑动另有其人：面板自己带着内联 `transition: width 500ms`，
标题栏的 padding 消费者也自带过渡 —— 根上的 var 过渡对视觉纯属冗余。

掐掉它（`transition-property: none !important`，important 压得过内联），
再给唯一没自带过渡的 var 消费者补一条同参数的过渡，
同样的 500ms 平滑动画就从「P95 67.5ms、9 个长帧、只见 8 格」
变成「P50 6.1ms 跑满 165Hz、19~50 格」。「啪」地到位的方案随之下岗。

三个教训：

- **layout 动画卡不卡，取决于每帧在重算什么**。把 recalc / layout / script
  分开归因（CDP `Performance.getMetrics` 前后相减），拆得掉成本就平滑，
  拆不掉再降级成「啪」。
- **在根节点上给 `inherits: true` 的注册属性做 transition 是性能陷阱**，
  动画应该放在消费它的元素自己的属性上。遇到「面板类动画莫名卡」，
  先看根元素的内联 style 里有没有 `transition: --xxx`。
- **掐之前把 var 的消费者列全**（搜内联 style 和样式表里的 `var(--xxx`）：
  谁没自带过渡，var 一停它就会瞬跳，要给谁补一条。

---

## 架构为什么这么分

```
patch\pixelTheme.js     →  打进 app.asar 里（改了要重新打包）
pixel-theme\*           →  放在 asar 外面（随时改，Ctrl+R 生效）
```

故意把**样式和资源放在 asar 外面**，只把一个几十行的加载器打进包里。
好处是调配色的时候不用反复拆包打包——改 CSS，Ctrl+R，看效果，几秒一轮。

注入器本身做了全链路容错：字体读不到、CSS 读不到、`setTitleBarOverlay` 抛异常，
统统 catch 住降级成原版行为，**绝不会因为主题的问题导致应用起不来**。
安装脚本也一样：先备份再动手，打包失败原文件不动。
