# 🛠️ Antigravity Theme 深度复用与二次开发指南

这套美化增强方案**具有极高的工程通用性与模块化解耦特性**，既支持在 Antigravity 内部随心定制、切换与新增主题，也能作为一套通用的 Electron 客户端注入方案，移植到任意基于 Chromium / Electron 架构的桌面应用（如 VS Code、Cursor、Windsurf、Slack 等）。

---

## 🧭 复用场景总览

| 场景 | 操作目标 | 涉及文件 | 耗时 |
|:---|:---|:---|:---|
| **A. 客户端更新后恢复** | Antigravity 官方更新覆盖了 `app.asar`，一键重补丁 | 运行 `install.bat` | 30 秒 |
| **B1. 调整既有主题配色/细节** | 改调色板、关闭扫描线、调节动效缓动 | `%LOCALAPPDATA%\...\resources\<theme>\<theme>.css` | 1~5 分钟 (按 Ctrl+R 即时生效) |
| **B2. 新增第 5 套全新风格主题** | 拓展新艺术风格（如赛博朋克、极简白墨、蒸汽波等） | 新建 `<theme>-theme/` 并在脚本中注册元数据 | 15~30 分钟 |
| **C. 移植到其他 Electron 应用** | 美化其他桌面应用 | 适配 `install.ps1` 6 个注入锚点与 IPC 守卫 | 半小时至数小时 |

---

## 🚀 A. Antigravity 更新后重新装回

### 运作机制
Antigravity 自动更新会从官方服务器下载全新的 `app.asar` 并覆盖原有文件，因此注入代码会被还原为官方版本。这是**唯一**需要定期执行的维护操作：

```powershell
# 一键以默认主题重装（若程序在后台，加 -KillRunning 自动强杀）
powershell -ExecutionPolicy Bypass -File .\install.ps1 -KillRunning

# 或以指定主题重装并激活
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Theme phantom -KillRunning
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Theme matcha -KillRunning
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Theme doodle -KillRunning
powershell -ExecutionPolicy Bypass -File .\install.ps1 -Theme pixel -KillRunning
```

### 幂等性安全设计
`install.ps1` 具备严格的**状态自检与幂等性保障**：
1. **已打补丁状态**：检测 `dist/utils.js` 是否已包含注入钩子。若注入器 `patch/pixelTheme.js` 未改动，则**仅刷新外部主题静态资源**，不重新拆包封包；若注入器代码发生变动，则自动同步更新并封包。
2. **全新原版状态**：自动将官方 `app.asar` 备份为 `app.asar.pixel-backup`，确保后续可以 100% 无损还原。
3. **安全熔断**：若新版客户端大幅度重构导致锚点未匹配，脚本会输出具体未命中的锚点并**直接退出，绝不产生破坏性的半修改状态**。

---

## 🎨 B. 换配色、换字体与新增全新主题

### 1. 调整已有主题配色 (零编译，实时见效)
所有主题的样式表均存放在外部目录中，为**纯文本标准 CSS**：
```
%LOCALAPPDATA%\Programs\Antigravity\resources\<theme-name>\<theme-name>.css
```
打开对应的 CSS 文件，定位到顶部的 `:root` 变量声明区：
```css
:root {
  /* 基础调色板 */
  --px-bg:       #1a1c2c;   /* 主背景色 */
  --px-fg:       #f4f4f4;   /* 主文字前景色 */
  --px-accent:   #73eff7;   /* 核心强调色/高亮光标/活动标签 */
  --px-border:   #566c86;   /* 边框分割线颜色 */
  ...
}
```
> **修改技巧**：保存 CSS 后，直接在 Antigravity 窗口中按下 **`Ctrl + R`** 刷新界面，耗时不到 1 秒即可看到新色彩。调出满意的配色后，记得把 CSS 文件复制回项目目录（如 `pixel-theme/pixel.css`），避免重装时被覆盖。

---

### 2. 替换与添加自定义字体
1. 准备好目标字体的 `.woff2` 格式文件，放入对应主题的 `fonts/` 文件夹下（例如 `matcha-theme/fonts/`）；
2. 打开 `patch/pixelTheme.js`，在顶部修改或注册 `FONTS` 映射：
   ```javascript
   const FONTS = {
       ui:   { file: "your-ui-font.woff2", family: "YourUIFontName" },
       mono: { file: "your-mono-font.woff2", family: "YourMonoFontName" },
   };
   ```
3. 在对应的 `.css` 文件中，将 `--px-font-ui` 与 `--px-font-mono` 变量更新为对应的字体族名；
4. 运行 `install.bat` 重新部署静态资源。

---

### 3. 如何新增第 5 套全新主题（完整流程）

假设我们要新增一套名为 **`cyberpunk`**（赛博朋克风）的主题：

#### 第一步：创建主题资源目录
在项目根目录下新建文件夹 `cyberpunk-theme/`：
```text
cyberpunk-theme/
├── cyberpunk.css      # 主题的核心样式文件
└── fonts/            # 该主题所需的 woff2 字体与许可文本
```

#### 第二步：编写主题样式规则
创建 `cyberpunk.css`，先从现有主题（如 `phantom.css`）拷贝基础结构：
- **第一节**：覆盖全局色彩语义系统（`--background`, `--foreground`, `--primary`, `--border` 等）；
- **第二节**：覆盖 Tailwind / VS Code 专用变量；
- **第三节**：定义组件直角/边框/光标动效规范。

#### 第三步：在 `switch-theme.ps1` 中注册主题元数据
打开 `switch-theme.ps1`，在 `$ThemesMeta` 哈希表中添加新主题定义：
```powershell
$ThemesMeta = @{
    "phantom"   = @{ Name = "phantom-theme";   Title = "PHANTOM GRAVITY (Persona 5 潮酷怪盗波普风)"; Color = "Red";     File = "phantom.css" }
    "matcha"    = @{ Name = "matcha-theme";    Title = "MATCHA GRAVITY (治愈系抹茶日记手帐风)";    Color = "Green";   File = "matcha.css" }
    "doodle"    = @{ Name = "doodle-theme";    Title = "DOODLE GRAVITY (纯线稿漫画粉印手绘风)";    Color = "Magenta"; File = "doodle.css" }
    "pixel"     = @{ Name = "pixel-theme";     Title = "PIXEL GRAVITY (8-Bit 复古像素极客风)";      Color = "Yellow";  File = "pixel.css" }
    # 新增项：
    "cyberpunk" = @{ Name = "cyberpunk-theme"; Title = "CYBER GRAVITY (夜之城霓虹赛博朋克风)";     Color = "Cyan";    File = "cyberpunk.css" }
}
```
并在顶部参数的 `[ValidateSet(...)]` 中加入 `"cyberpunk"`。

#### 第四步：在 `install.ps1` 中加入部署列表
打开 `install.ps1`，更新 `$AllThemes` 数组及参数校验列表：
```powershell
$AllThemes = @('pixel-theme', 'doodle-theme', 'matcha-theme', 'phantom-theme', 'cyberpunk-theme')
```

#### 第五步：部署并享受秒级热切
运行 `powershell -ExecutionPolicy Bypass -File .\install.ps1 -Theme cyberpunk -KillRunning` 完成首次部署。后续即可通过 `switch cyberpunk` 实现 0.1 秒免重启秒切！

---

## 🧩 C. 拿去美化其他 Electron 应用

这套方案的底层注入逻辑是纯通用的。只需搞清宿主应用的结构，即可将其移植到任意基于 Electron 的现代软件中。

### 1. 目标应用前置可行性评估

在动工前，请依次排查以下三项指标：

#### 检查 1：确认为 Electron 应用
查看安装目录，若包含 `resources\app.asar` 以及 `ffmpeg.dll`、`libGLESv2.dll` 等依赖，即为标准 Electron 架构。

#### 检查 2：asar 完整性校验状态（核心前提）
在 PowerShell 中运行以下探测脚本，检查应用二进制是否锁死了 asar 校验：
```powershell
node -e "const fs=require('fs');const b=fs.readFileSync('目标\\App.exe');const i=b.indexOf('dL7pKGdnNz796PbbjQWNKmHXBZaB9tsX');const names=['RunAsNode','EnableCookieEncryption','EnableNodeOptionsEnvironmentVariable','EnableNodeCliInspectArguments','EnableEmbeddedAsarIntegrityValidation','OnlyLoadAppFromAsar','LoadBrowserProcessSpecificV8Snapshot','GrantFileProtocolExtraPrivileges'];const c=b[i+33];for(let k=0;k<c;k++)console.log(names[k]||k,'=',String.fromCharCode(b[i+34+k]))"
```
- 若 `EnableEmbeddedAsarIntegrityValidation = 0`：**完全可修改**（Google Antigravity 即为 0）。
- 若 `EnableEmbeddedAsarIntegrityValidation = 1`：应用在启动时会校验 asar 哈希，篡改后会拒绝运行（需对可执行文件执行反校验 Patch）。

#### 检查 3：界面呈现方式
界面由 Web 技术渲染（HTML/CSS/JS），打开应用调试端口能抓取 DOM 结构即可。

---

### 2. 六大注入锚点与 IPC 守卫详解

解开宿主应用的 `resources\app.asar`：
```powershell
npx @electron/asar extract "目标\resources\app.asar" .\extracted_app
```
在解开的代码中全局搜索 `new BrowserWindow`，定位创建主窗口的核心文件（在 Antigravity 中为 `dist/utils.js`）。

`install.ps1` 定义了 **6 个核心定位锚点** 和 **1 个 IPC 守卫**：

| 锚点标号 | 定位特征 / 原始内容 | 替换操作与工程目的 | 关键说明 |
|:---|:---|:---|:---|
| **`$A1`** | `const loadingOverlay_1 = require("./loadingOverlay");` | 追加 `const pixelTheme_1 = require("./pixelTheme");` | 在主进程中引入自研主题注入模块 |
| **`$A2`** | `(0, loadingOverlay_1.attachLoadingOverlay)(win, ...);` | 替换为自定义启动动画加载钩子 | 将应用冷启动时的默认加载菊花换成定制动画 |
| **`$A3`** | `void win.loadURL(url);` | 在该行前插入 `(0, pixelTheme_1.attachPixelTheme)(win);` | **最核心锚点**：在页面正式导航前挂载 DOM/CSS 注入器 |
| **`$A4`** | `const backgroundColor = isLight ? '#FAFAFA' : '#131313';` | 替换为 `chromeColors(isLight).background` | 将窗口初始底色同步为主题背景，**杜绝开窗浅白瞬间闪烁** |
| **`$A5`** | `const foregroundColor = isLight ? '#383A42' : '#FAFAFA';` | 替换为 `chromeColors(isLight).foreground` | 同步窗口初始前景色 |
| **`$A6`** | `titleBarOverlay: isMacOS()` | 替换为关闭系统原生按钮逻辑 | 规避 Windows 11 `titleBarOverlay` 底色无法自定义的 Bug |
| **`$H1`** (IPC) | `if (win && process.platform === 'win32') {` (在 `ipcHandlers.js`) | 追加 `&& !win.__pixelNoNativeCaption` 判断 | 防止应用自身周期性刷新系统标题栏时抛出 `Titlebar overlay is not enabled` 异常刷屏 |

---

## 🛡️ 踩坑实录与高刷调优经验（照抄可省数天调试）

以下 23 条均为实战过程中踩坑总结出的宝贵经验：

### 1. 勿使用 `[class*="border"]` 粗暴加粗边框
Tailwind 中 `border-l` 代表仅有左边框。若用属性模糊匹配 `[class*="border"]`，会导致原本单边的元素凭空长出上下右三条边。
- **正确方案**：使用类名精确列表匹配 `[class~="border"]`，针对单边规则单独覆盖（如 `[class~="border-t"]`）。

### 2. 严防空浮层撑出系统滚动条
现代前端框架中常存在宽高均为 0×0 的常驻定位占位符（如 ComboBox 联想菜单锚点）。若全局赋予其 `border: 2px`，尺寸变为 4×4，会在文档底层产生溢出，导致整个窗口出现滚动条，破坏固定布局。
- **双重保险**：
  ```css
  /* 仅对非空浮层生效 */
  [role="listbox"]:not(:empty) { border: 2px solid var(--border) !important; }
  /* 焊死桌面客户端根容器滚动条 */
  html, body { overflow: hidden !important; width: 100% !important; height: 100% !important; }
  ```
- **2026-09-17 补充（实机复现）**：本仓库实际用的是打标类 `.px-menu-content`（注入器给 `[role=menu]/[role=listbox]` 打的桶类），它没有 `:not(:empty)` 这道闸门 —— 应用的 typeahead 常驻一个**空**的 `[role="listbox"]` 壳，inline 只有 `display/position`、没有 `top/left`，于是按 static position 停在 `#root` 底边（y = 视口高度）；主题给的 2px 边框 + 4px 上下内边距把它从 0 撑到 11px，根滚动条照旧出现。而且 `html { overflow: hidden }` **挡不住程序化滚动**（见第 21 条），一次 `focus()` / `scrollIntoView()` 就把根滚掉 11px，整个 UI 上移、顶部菜单栏被裁。空壳没有任何视觉，必须显式归零：
  ```css
  html body [role="menu"]:empty,
  html body [role="listbox"]:empty,
  html body .px-menu-content:empty {
    border: 0 !important; padding: 0 !important; box-shadow: none !important;
    animation: none !important; min-height: 0 !important; height: 0 !important;
  }
  ```

### 3. `scrollbar-color` 会直接废除 `::-webkit-scrollbar`
在 Chromium 引擎中，一旦节点被声明了 CSS 标准属性 `scrollbar-color` 或 `scrollbar-width`，所有针对 `::-webkit-scrollbar` 的精细伪元素样式均会立即失效，且该属性具有继承性。
- **排错解法**：在注入样式表顶部重置：
  ```css
  html, body, body * { scrollbar-color: auto !important; scrollbar-width: auto !important; }
  ```

### 4. 像素字体必须显式关闭平滑抗锯齿
点阵位图字体若开启亚像素渲染会边缘模糊发虚。
- **解法**：`html, body, body * { -webkit-font-smoothing: none !important; }`。

### 5. 点阵字体切忌加粗 (`font-weight: bold`)
点阵字体没有真粗体，浏览器会触发几何算法加粗（Faux Bold），导致点阵笔画粘连。
- **解法**：将标题与加粗标签强制降为 400，依靠高亮色彩建立视觉层级：
  ```css
  b, strong, h1, h2, h3, [class*="font-bold"] { font-weight: 400 !important; }
  ```

### 6. Windows 11 `titleBarOverlay` 底色锁死缺陷与自绘方案
在 Windows 11 Build 22621+ 及 Electron 30+ 环境下，系统原生的 `titleBarOverlay` 窗口控制按钮底色在特定系统主题下无法被 `setTitleBarOverlay({ color })` 正确着色，始终显示灰白底块。
- **解法**：将 `titleBarOverlay` 设为 `false`，由页面自绘最小化、最大化与关闭按钮，调用 Preload 中暴露的 `window.electronNative.minimize()` 等 API 驱动，实现 100% 视觉融合。

### 7. 正则扫描配置务必先剥离 CSS 注释
若主进程需要从 CSS 中提取配置变量，务必先剔除 `/* ... */`，否则注释中的示例值（如 `改成 on 生效`）会被正则误抓，导致逻辑错误。

### 8. 坚决避免 PowerShell 5.1 `Set-Content -Encoding utf8` 产生 BOM
Windows PowerShell 5.1 默认的 UTF-8 会强行写入 3 字节的 UTF-8 BOM 头，Chromium 样式表加载器在遇到带 BOM 的外部样式时可能发生解析异常。
- **解法**：在 PowerShell 中使用 `[System.Text.UTF8Encoding]($false)` 显式写入无 BOM 文件。

### 9. Preload 中通过 `contextBridge` 暴露的对象是深度冻结的
无法在渲染进程中通过原型链篡改已暴露给 `window` 的原生 API。如需修改底层行为，必须在主进程侧完成拦截。

### 10. 避免使用 `scale()` 动效破坏点阵字
缩放动画（Transform Scale）会导致位图字体在插值期间发生采样形变与模糊。展开动画应统一使用整数像素的 `translate` 位移。

### 11. 区分便宜动效与贵动效
- **便宜属性**：`transform`、`opacity`（由 GPU 合成器线程承载，极度流畅）。
- **高危属性**：`width`、`height`、`top`、`margin`（每帧均引发 Reflow 全树重排）。对于大面积面板，要么缩短动画时长（如 70ms 瞬时到位），要么拆解重排根因。

### 12. 位移使用独立的 `translate` 属性而非 `transform`
使用 CSS 独立属性 `translate: x y` 可以与元素原本已有的 `rotate()` 或 `scale()` 矩阵自然叠加，避免关键帧直接覆盖元素原有的变形。

### 13. `!important` 优先级高于 CSS 动画关键帧
若元素已被 `!important` 声明了某属性（如 `border-color`），关键帧动画中的同属性将无法生效。可改用未被占用的替代属性（如 `filter: brightness(...)`）实现动态闪烁。

### 14. `webContents.insertCSS` 不会出现在 `document.styleSheets` 中
通过 Electron WebFrame 注入的 CSS 运行在独立的层级，排查样式命中情况时切勿以 `document.styleSheets` 为准，必须直接测量元素的 `getComputedStyle()`。

### 15. 规避高频触发的 `:has()` 伪类选择器
`:has()` 会导致子树任何 DOM 节点的微小变动都向上层触发级联失效。在流式输出（Streaming Tokens）等高频 DOM 插入场景下，尽量选用直接父子类名选择器替代。

### 16. 空闲性能检测忌用 `requestAnimationFrame` 死循环
rAF 会强行唤醒渲染流水线，制造出虚假的高 CPU 占用。真实空闲开销应通过 CDP 定时采样两次 `Performance.getMetrics` 的指标差值来衡量。

### 17. 警惕在 `:root` 上对注册 `@property` 变量执行 Transition
若应用在根节点针对注册为 `inherits: true` 的自定义 CSS 变量执行过渡动画（如 `transition: --sidebar-width 500ms`），会导致整棵 DOM 树的计算样式在每一帧全量失效，引发严重的长任务（Long Tasks）阻塞。
- **解法**：在注入样式中对根节点强制设置 `transition-property: none !important;`，将过渡动画拆分并直接挂载在消费该变量的具体容器节点上，瞬间化解掉帧。

### 18. CDP 0-重启热重载机制 (Zero-Restart CDP Reload)
`switch-theme.ps1` 不需要重启客户端，其技术原理为：
1. 客户端以调试模式或默认状态运行，其端口会自动记录在 `%APPDATA%\Antigravity\DevToolsActivePort`；
2. 切换脚本读取该端口并通过 WebSocket 发送 `Page.reload` 或向页面派发定制的 `StyleSheet` 替换指令；
3. 渲染页面就地完成刷新，耗时通常仅为 0.1~0.3 秒，当前会话与上下文完全保留。

### 19. 外部多主题目录隔离与配置持久化
通过在 `resources/` 目录下生成 `active-theme.json` 记录当前激活主题，主进程启动注入器自动读取该文件，实现了样式与代码彻底解耦。即便外部某个 CSS 文件存在语法瑕疵，注入器全链路配有 `try-catch` 降级逻辑，确保宿主应用永不白屏崩溃。

### 20. 设备像素对齐 (`calc(N * var(--px-dev))`)
在 2K / 4K 高分屏（DPI 缩放 125%、150%、200%）环境下，`1px` 的 CSS 像素不等于整数物理像素。注入器在运行时自动向 `document.documentElement` 写入 `--px-dev: (1 / window.devicePixelRatio)px`。所有点阵描边与微位移统一以此为基准，确保在任何高分屏缩放倍率下均保持真 1 物理像素的刀锋般锐利质感。

### 21. 根滚动条位移：`overflow: hidden` 挡不住，要 `clip`
`html { overflow: hidden }` 只是「不显示滚动条」，根元素**仍是可被程序化滚动的滚动容器**。只要文档存在哪怕 11px 溢出（例如主题给空浮层加的边框/内边距，见第 2 条），任何一次 `focus()` / `scrollIntoView()` 都能把根滚掉一截：页面内所有非 fixed 元素整体上移（顶部 35px 菜单栏被裁掉一块），而 `position: fixed` 的自绘窗口按钮纹丝不动 —— 表现为「标题栏超框/错位」，且只在某些操作之后偶发。
- **解法**：`html { overflow: clip !important; }`。`clip` 不建立滚动容器，程序化写 `scrollTop` 直接无效；再配合「空浮层归零」从源头消掉溢出。
- **验证**：`document.documentElement.scrollHeight === clientHeight`，且写入 `scrollTop = 20` 后读回仍是 `0`。

### 22. 自定义指针画布与弹层 z-index 打平会输
自绘像素指针是一张 `position: fixed; z-index: 2147483647` 的全屏 canvas。若某个弹层也被提升到同一个 2147483647（例如 `div:has(> [role="listbox"]:not(:empty))` 这类提升规则），**打平后按 DOM 顺序决胜**：画布挂载时排在 `body` 前部、弹层是后挂载的 portal —— 弹层盖住画布，而原生指针又被 `cursor: none` 全局隐藏，用户看到的就是「悬停在下拉菜单上完全没有指针」。
- **铁律**：任何弹层（popper wrapper / menu / listbox / dialog / tooltip）的 `z-index` 必须**严格小于** 2147483647，最高层只留给指针画布；画布自身的层级也别靠「打平赢 DOM 顺序」去赌。
- **验证**：比对 `getComputedStyle(弹层).zIndex` 与 `#px-cursor`（实机为 2147483640 vs 2147483647），并截一张全屏图确认指针确实画在菜单之上。

### 23. 勾选态开关带着 `bg-primary`，会被主按钮规则误伤
应用的开关就是 `button[role="switch"]`，**勾选时类名带 `bg-primary`**，于是同时命中「主按钮 = 方角 + 硬阴影」的整套规则：同一个开关勾选后变方角硬阴影、未勾选还是圆角胶囊，看起来像两个不同控件。
- **解法**：给开关加一条兜底，放进最先登记的 `px-theme` 层并带 `!important` —— 带层 important 压得过一切无层 important，不必和主按钮规则做特异性拉锯：
  ```css
  @layer px-theme {
    html body button[role="switch"],
    html body button[role="switch"]:hover,
    html body button[role="switch"]:focus-visible,
    html body button[role="switch"]:active {
      border-radius: 9999px !important;   /* 像素主题用 0，与自家开关分节保持一致 */
      box-shadow: none !important;
    }
    html body button[role="switch"] > span {
      width: 16px !important; height: 16px !important;
    }
  }
  ```
  旋钮尺寸必须留在应用自带的 `h-4 w-4`（16px）：应用是用 `translateX(4px / 24px)` 定位旋钮的，16px 时左右内边距才是对称的 4px（改成 18px 会让勾选态右侧只剩 2px）。
- **同类风险**：任何带语义类（`bg-primary` / `bg-secondary` / `cursor-pointer`）的复合控件（开关、复选框、单选、滑块）都可能被主按钮/卡片规则连带命中。给这类控件写样式前，先在实机量一遍它的 computed style 再动手。